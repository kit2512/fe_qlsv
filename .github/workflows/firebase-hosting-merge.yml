# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
    
      - run: sudo snap install flutter --classic && flutter channel stable && flutter config --enable-web && dart pub global activate melos && melos bs
      - run: export PATH="$PATH":"$HOME/.pub-cache/bin"
      - run: cd apps/admin && flutter build web --release
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_QLSV_960A4 }}'
          channelId: live
          projectId: qlsv-960a4
          target: admin
      - run: cd apps/admin && flutter build web --release
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_QLSV_960A4 }}'
          channelId: live
          projectId: qlsv-960a4
          target: student
          
      
